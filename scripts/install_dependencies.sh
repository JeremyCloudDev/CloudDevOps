#!/bin/bash

# Actualiza el sistema e instala dependencias necesarias
sudo apt-get update
sudo apt-get install -y nginx # Cambia esto por los paquetes que necesites

# Aquí puedes agregar otros comandos para instalar más dependencias o configurar el entorno
echo "Dependencias instaladas correctamente."